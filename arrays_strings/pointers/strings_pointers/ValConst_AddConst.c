#include<stdio.h>
int main()
{
    const char * const p ="kids";
    printf("\n %s ",p);
    // p[0]='s'; // assignment to read only variable :
    // p="Natasha"; // assignment to read only variable :
    // we can modify the address assigned to the Pointer:
}
